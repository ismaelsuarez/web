{
  "name": "ecommerce-monorepo",
  "version": "1.0.0",
  "description": "Monorepo para aplicación de ecommerce con frontend y backend",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "bootstrap": "pnpm install && pnpm run build",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "backend:dev": "pnpm --filter backend start:dev",
    "backend:prod": "pnpm --filter backend start:prod",
    "backend:db:generate": "pnpm --filter backend db:generate",
    "backend:db:push": "pnpm --filter backend db:push",
    "backend:db:seed": "pnpm --filter backend db:seed"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.45.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.1.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.0.0"
}
